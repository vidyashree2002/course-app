export const BASE_URL = 'http://localhost:3000';
//The purpose of a config file is to provide a centralized location for managing these settings, making
//  it easier to customize and control the behavior of the application without modifying its code.

// During deployment, you can modify the config file to point to different URLs or resources without
//  modifying the rest of your codebase. This is especially useful when deploying to different environments.